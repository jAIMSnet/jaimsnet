# Ansible Roles

Reusable Ansible roles for common configuration tasks.

## Roles

| Role | Purpose |
|------|---------|
| `common` | Base system configuration (packages, users, SSH) |
| `docker` | Docker Engine installation and configuration |
| `firewall` | UFW firewall rules |
| `nvidia` | NVIDIA GPU drivers and container runtime |
